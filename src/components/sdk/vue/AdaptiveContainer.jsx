// Vue SDK component — reference implementation only, not used by the React app.
// See vue-sdk/AdaptiveContainer.vue for the actual Vue source.
export default function AdaptiveContainer() { return null; }